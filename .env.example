# Livepeer Agent — mock = free local placeholders (default), real = keyless creative MCP.
# REAL MODE SPENDS REAL BUDGET: $100 shared across ALL hackers, re-ups every 24h. Be conservative.
LIVEPEER_MODE=mock
LIVEPEER_MCP_URL=https://agent.livepeer.org/api/mcp/creative

# OriginTrail DKG — file = local JSON-LD snapshots (dev), http = local edge node API.
# Testnet publishing (Knowledge Assets get UALs) comes via the edge node in http mode.
DKG_MODE=file
DKG_NODE_URL=http://127.0.0.1:9200
DKG_TOKEN=

PORT=3111
